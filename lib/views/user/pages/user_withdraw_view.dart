import 'package:flutter/material.dart';

class UserWithdrawView extends StatelessWidget {
  ///
  ///
  const UserWithdrawView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
    );
  }
}
